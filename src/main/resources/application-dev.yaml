# Dev profile: disable built-in static resource serving — Vite dev server handles it.
spring:
  web:
    resources:
      add-mappings: false
  devtools:
    restart:
      enabled: true
      # Additional paths watched for changes
      additional-paths: /workspace/classes,/workspace/resources
